// Isolated idler bracket test — for visual verification
include <config_v5_5.scad>
use <monolith_v5_5.scad>

$fn = 32;

// Render just the idler brackets + nearby junction for context
_all_idler_brackets();
